function add(x, y){
  if(isNaN(x)) return console.log('Input must be number.');
  return x + y;
}

function sub(x, y){
  if(isNaN(x)) return console.log('Input must be number.');
  return x - y;
}

function mul(x, y){
  if(isNaN(x)) return console.log('Input must be number.');
  return x * y;
}
